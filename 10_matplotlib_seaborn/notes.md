# 第10章 理解度チェック — Matplotlib / Seaborn

## チェックリスト

- [ ] `plt.plot()` で折れ線グラフが描ける
- [ ] `scatter()`, `bar()`, `hist()` が使える
- [ ] `subplots()` で複数グラフを並べられる
- [ ] `title`, `xlabel`, `ylabel`, `legend` を設定できる
- [ ] Seaborn で `heatmap()` や `pairplot()` が使える
- [ ] `savefig()` でファイルに保存できる
